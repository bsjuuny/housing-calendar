# 🏠 Housing Calendar (내 집 마련 캘린더) 개발자 가이드

안녕하세요! 이 프로젝트는 공공분양이나 임대주택 청약 일정을 놓치지 않도록 일목요연하게 정리해서 보여주고 알림을 보내주는 서비스입니다.

---

## 🏗 시스템 구성 및 핵심 기능

1.  **데이터 수집**: 공공데이터포털 등에서 청약 일정을 긁어와서 `data/housing_events.json`에 저장합니다.
2.  **클라이언트 사이드 편의성**: 검색 성능을 위해 모든 데이터 필터링은 브라우저(클라이언트)에서 즉시 일어나며, **Zustand**를 사용해 '관심 단지 즐겨찾기' 기능을 구현했습니다.
3.  **알림**: 매일 아침 당일 오픈되는 청약이나 마감 임박 건을 텔레그램으로 콕 집어서 보내줍니다.

---

## 🛠 주요 명령어 및 작업 안내

- `npm run dev`: 캘린더 화면이나 카드 디자인을 고치고 싶을 때 사용하세요.
- `scripts/telegram-notifier.mjs`: 알림 메시지 문구(예: "오늘의 청약 알림")를 수정하고 싶을 때 이 파일을 열어보세요.

---

## 💡 개발자를 위한 유지보수 팁

### '관심 단지'가 왜 안 사라지나요? (Persistence)
- 이 프로젝트는 **Zustand의 persist 미들웨어**를 사용합니다. 즉, 사용자가 즐겨찾기한 정보는 브라우저의 `localStorage`에 저장되어 웹을 껐다 켜도 그대로 유지됩니다.

### 애니메이션 수정
- 카드 클릭 시 부드럽게 커지거나 움직이는 효과는 `framer-motion`을 썼습니다. 혹시 움직임이 너무 느리다고 느껴지면 `transition` 값을 조정하세요.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 청약 정보가 옛날 데이터만 나와요.**
- A: 중앙 스케줄러(`c:/github/scheduler`)가 매일 아침 수집 스크립트(`run-housing-calendar.mjs`)를 제대로 돌렸는지 확인해야 합니다.

**Q: 특정 단지가 검색이 안 돼요.**
- A: 공공데이터 API에서 데이터가 누락되었거나, `public/data/housing_events.json` 파일에 해당 키워드가 포함되어 있지 않을 수 있습니다.

---

## 📁 주요 폴더 설명
- `src/stores/`: 즐겨찾기 상태 관리를 담당하는 Zustand 스토어 위치.
- `scripts/`: 데이터 수집(Scraper) 및 알림(Notifier) 로직.
- `src/app/`: 캘린더와 상세 정보를 보여주는 Next.js 페이지.
